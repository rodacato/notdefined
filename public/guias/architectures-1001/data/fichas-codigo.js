/* ============================================================================
   fichas-codigo.js — Fichas profundas · Familia 5 (Organización del código)
   ----------------------------------------------------------------------------
   Sin fichas profundas todavía: monorepo y polyrepo traen solo la semilla de
   catálogo. Para expandir uno, marca hasFicha/hasDiagrama en catalogo.js y agrega
   su bloque aquí siguiendo el formato de fichas-despliegue.js.
   ========================================================================== */
(function (G) {
  G.fichas = G.fichas || {};
  // (sin fichas profundas en esta familia)
})(window.GUIA = window.GUIA || {});
