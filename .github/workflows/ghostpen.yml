name: Ghostpen — AI Blog Post

on:
  issues:
    types: [labeled]

# Runs when 'ghostpen' (blog post) or 'ghostpen-til' (TIL) label is applied.

permissions:
  contents: write
  issues: write
  pull-requests: write
  models: read

jobs:
  ghostpen:
    if: github.event.label.name == 'ghostpen' || github.event.label.name == 'ghostpen-til'
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Generate draft
        id: ghostpen
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          ISSUE_TITLE: ${{ github.event.issue.title }}
          ISSUE_BODY: ${{ github.event.issue.body }}
          ISSUE_NUMBER: ${{ github.event.issue.number }}
          POST_TYPE: ${{ github.event.label.name == 'ghostpen-til' && 'til' || 'blog' }}
        run: node .github/scripts/ghostpen.mjs

      - name: Commit draft and open PR
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          ISSUE_TITLE: ${{ github.event.issue.title }}
          ISSUE_NUMBER: ${{ github.event.issue.number }}
          POST_TYPE: ${{ github.event.label.name == 'ghostpen-til' && 'til' || 'blog' }}
        run: |
          git config user.name "ghostpen[bot]"
          git config user.email "ghostpen@users.noreply.github.com"

          git checkout -b "${{ steps.ghostpen.outputs.branch }}"
          git add "${{ steps.ghostpen.outputs.file }}"
          git commit -m "feat(content): ghostpen $POST_TYPE draft — ${{ github.event.issue.title }}"
          git push origin "${{ steps.ghostpen.outputs.branch }}"

          gh pr create \
            --title "ghostpen($POST_TYPE): ${{ github.event.issue.title }}" \
            --body "$(cat <<'EOF'
          AI-generated draft from the ghostpen pipeline.

          Review the post, edit as needed, and merge when ready. The issue will be closed automatically on merge.

          Closes #${{ github.event.issue.number }}
          EOF
          )" \
            --head "${{ steps.ghostpen.outputs.branch }}" \
            --base master
